#ifndef RECURSION_H
#define RECURSION_H

#include <vector>
// you SHOULD NEVER use namespace std in a header file!!!

double pow(double x, int n);
int binarySearch(const std::vector<int>& v, int val, int lo, int hi);

#endif /* end of include guard: RECURSION_H */
